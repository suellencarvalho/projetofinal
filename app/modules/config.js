função  executada ( $ rootScope , $ state , $ location ) {
    // $ rootScope. $ on ('$ stateChangeStart', função (evento, próximo, nextParams, toState) {
    //      if (toState.name! == "auth.signin" || toState.name === null) {
    //          $ location.path ("auth / signin");
    //      }
    // });
}

angular . módulo ( ' todoApp ' ). executar (executar);